apiVersion: v1
kind: Service
metadata:
  name: { { .Values.name | default "loansdb" } }
spec:
  selector:
    app: { { .Values.name | default "loansdb" } }
  ports:
    - port: 3306
      targetPort: 3306
